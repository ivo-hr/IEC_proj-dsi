<Page
    x:Class="dsi_mockup_pero_en_xaml_xd.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:dsi_mockup_pero_en_xaml_xd"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
       
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Canvas>
            <Image Stretch="Fill" Source="/Assets/fondo.png" Height="1009" Width="1500"/>
        </Canvas>
        
        <StackPanel Grid.Column="1" Margin="40,0,0,0" VerticalAlignment="Center">
            <TextBlock Foreground="White" FontSize="70" Text="HEROES DEL TODAY "/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,0" VerticalAlignment="Center">
            <Button Content="Empesar"
                Click="Page1Button_OnClick"
                Grid.Row="1"
                Margin="40,40,40,40" Height="100" Width="320" VerticalAlignment="Top" />
        <Button Content="Continuar"
            Click="Page1Button_OnClick"
            Grid.Row="1"
            Margin="40,40,40,40" Height="100" Width="320" VerticalAlignment="Top" />
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,0,0,0" VerticalAlignment="Center">
            <Button Content="Options"
                Click="Page1Button_OnClick"
                Grid.Row="1"
                Margin="40,40,40,20" Height="50" Width="200" VerticalAlignment="Top" />
            <Button Content="Credits"
                Click="Page1Button_OnClick"
                Grid.Row="1"
                Margin="40,20,40,20" Height="50" Width="200" VerticalAlignment="Top" />
        </StackPanel>
    </Grid>
</Page>
